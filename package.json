{
  "name": "pig-latin",
  "version": "1.0.0",
  "description": "Hackathon starter code",
  "scripts": {
    "lint": "htmllint {*.html,./**/*.html} && stylelint ./**/*.css && eslint .",
    "test": "mocha --no-timeouts",
    "start": "cd . && http-server -c-1"
  },
  "author": "Clarissa Jaime",
  "dependencies": {
    "eslint": "^3.19.0",
    "htmllint-cli": "github:kevincolten/htmllint-cli",
    "http-server": "^0.11.1",
    "jsdom": "^11.6.2",
    "mocha": "^5.0.0",
    "postcss-html": "^0.34.0",
    "stylelint": "^7.13.0"
  }
}
